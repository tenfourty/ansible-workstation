---

- name: Configure my workstation using ansible-pull
  hosts: localhost
  connection=local
  user=root
  become=true
  become_method=sudo
  pre_tasks:
    - name: update packages
      package: name=* state=latest

  roles:
    - workstation
    - ansible-pull-cron

